{
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "start": "cross-env NODE_ENV=production node .",
    "predev": "yarn build && cd ./frontend && yarn install && yarn build",
    "dev": "cross-env NODE_ENV=development nodemon -e ts,tsc --exec \"tsc && nodemon --inspect .\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-frontend": "cd ../frontend && yarn install && yarn build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/filetrust/icap-management-ui.git"
  },
  "keywords": [
    "glasswall",
    "icap",
    "docker",
    "kubernetes"
  ],
  "author": "glasswallsolutions",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/filetrust/icap-management-ui/issues"
  },
  "homepage": "https://github.com/filetrust/icap-management-ui#readme",
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/node": "^14.10.2",
    "dotenv": "^8.2.0",
    "jest": "^24.9.0",
    "nodemon": "^2.0.6",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/node-fetch": "^2.5.7",
    "body-parser": "^1.19.0",
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "guid-typescript": "^1.0.9",
    "http": "^0.0.1-security",
    "node-fetch": "^2.6.1",
    "winston": "^3.3.3"
  }
}
